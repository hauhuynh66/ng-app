import { CbgDirective } from './cbg.directive';

describe('CbgDirective', () => {
  it('should create an instance', () => {
    const directive = new CbgDirective();
    expect(directive).toBeTruthy();
  });
});
